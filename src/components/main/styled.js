import styled from "@emotion/styled";


export const CardWrapper = styled('div')(() => ({
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'space-between', 
    gap: '20px', 
    maxWidth: '375px',
}));